import { useEffect, useState } from 'react';

const CompileScreen = (props) => {
  
  const compileStyle ={
    "backgroundColor": "antiquewhite",
    "position": "relative",
    "width":"100%",
    "height":"100%",
    "fontSize":"auto",
  }

  return (
    <div class="compile" style={compileStyle}>
      <p>※集計 管理画面（ただいま開発中）</p>
    </div>
  );
}

export default CompileScreen;

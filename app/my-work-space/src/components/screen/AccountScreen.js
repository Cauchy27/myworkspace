import { useEffect, useState } from 'react';

const AccountScreen = (props) => {
  
  const accountStyle ={
    "backgroundColor": "antiquewhite",
    "position": "relative",
    "width":"100%",
    "height":"100%",
    "fontSize":"auto",
  }

  return (
    <div class="account" style={accountStyle}>
      <p>※アカウント 管理画面（ただいま開発中）</p>
    </div>
  );
}

export default AccountScreen;
